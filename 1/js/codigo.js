//let nombre= prompt("Ingresar un Nombre")

/*
let alumnos = [];
let nota = [];
let suma = 0;

for (let index = 0; index < 3; index++) {
    alumnos.push(prompt('Ingresar un Nombre'))
    nota.push(prompt('Ingrese la nota de: ' + alumnos[index]))

    suma = suma + parseInt(nota[index]);
}

document.write('El promedio del curso es: ' + suma/nota.length)
*/

var x = 3;

switch(x){
    case 1:
    document.write(x);
    break;
    case 2:
    document.write(x+2);
    break;
    default:
    document.write(x+5);
}

